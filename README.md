# Usage

```
Usage: lookctl <command> [arguments]

Commands:
   current   Show the currently used theme, icon, and cursor
   list      Show installed themes
   set       Set the theme, icon, or cursor

Run 'lookctl <command> -h' for more information on a command.
```
